# Tutor-GPT OpenWebUI Pipe Configuration
# Copy this file to .env and fill in your values

# Memory Proxy Configuration
# URL where the Tutor-GPT memory proxy is running
TUTOR_GPT_PROXY_URL=http://localhost:8081

# API key for authenticating with the memory proxy
# IMPORTANT: This must match the PROXY_API_KEY in memory-proxy/.env
TUTOR_GPT_PROXY_API_KEY=your-secret-api-key-here

# Optional: These will be used as defaults in the Valves configuration
# They can be overridden through OpenWebUI's admin panel
# TIMEOUT_SECONDS=300
# DEBUG_MODE=false
